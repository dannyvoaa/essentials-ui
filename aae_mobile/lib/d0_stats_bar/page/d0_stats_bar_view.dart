/// This is a generated file that you can modify with the required fields
/// If you need to change the fields/ what data and functions should be in the new visual component do that in [D0StatsViewModel]

import 'package:flutter/material.dart';
import 'package:aae/d0_stats_bar/d0_stats_bar_view_model.dart';

class D0StatsView extends StatelessWidget {
  final D0StatsViewModel viewModel;

  D0StatsView(this.viewModel);

  @override
  Widget build(BuildContext context) => Container(child: Center(child: Text('Implement your custom view here')));
}

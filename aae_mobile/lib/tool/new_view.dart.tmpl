/// This is a generated file that you can modify with the required fields
/// If you need to change the fields/ what data and functions should be in the new visual component do that in [##class_name##ViewModel]

import 'package:flutter/material.dart';
import 'package:aae/##file_name##/##file_name##_view_model.dart';

class ##class_name##View extends StatelessWidget {
  final ##class_name##ViewModel viewModel;

  ##class_name##View(this.viewModel);

  @override
  Widget build(BuildContext context) => Container(child: Center(child: Text('Implement your custom view here')));
}
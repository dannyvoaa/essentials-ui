/// This is a generated file that you can modify with the required fields
/// If you need to change the fields/ what data and functions should be in the new visual component do that in [TravelTravelViewModel]

import 'package:flutter/material.dart';
import 'package:aae/travel/travel_view_model.dart';

class TravelTravelView extends StatelessWidget {
  final TravelTravelViewModel viewModel;

  TravelTravelView(this.viewModel);

  @override
  Widget build(BuildContext context) => Container(child: Center(child: Text('Implement your custom view here')));
}

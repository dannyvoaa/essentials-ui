import 'package:flutter/widgets.dart';

/// [AaePlaceholderWidgets] This class will allow you to add widgets that are
/// still not finalized in designed but need to see how it would be laid out
/// on screen.
class AaePlaceholderWidgets extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Placeholder();
}
